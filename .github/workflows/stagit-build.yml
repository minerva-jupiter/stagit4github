name: Build Stagit Portal

on:
  schedule:
    - cron: '36 */3 * * *'
  repository_dispatch:
    types: [repo_pushed]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Install dependencies & stagit
        run: |
          sudo apt-get update
          sudo apt-get install -y libgit2-dev gcc make
          git clone https://git.codemadness.org/stagit /tmp/stagit
          cd /tmp/stagit
          make -j$(nproc)
          sudo make install

      - name: Clone All Repositories
        env:
          GH_TOKEN: ${{ secrets.PORTAL_PAT }}
        run: |
          git config --global url."https://x-access-token:${GH_TOKEN}@github.com/".insteadOf "https://github.com/"
          
          mkdir -p repositories

          gh repo list --limit 1000 --json url -q '.[].url' | while read -r url; do
            [ -z "$url" ] && continue
            repo_name=$(basename "$url" .git)
            git clone --bare "$url" "repositories/${repo_name}.git"
          done

      - name: Generate Stagit HTML
        run: |
          mkdir -p html
          ROOT_DIR="$(pwd)"

          # 各 bare リポジトリの HTML ページを生成
          for repo_dir in "${ROOT_DIR}/repositories"/*.git; do
            [ -d "$repo_dir" ] || continue
            
            repo_name=$(basename "$repo_dir" .git)
            site_dir="${ROOT_DIR}/html/${repo_name}"
            
            # 1. 出力先ディレクトリを作成して cd
            mkdir -p "$site_dir"
            
            # 2. htmldir 内で stagit <path-to-repo> を実行
            (cd "$site_dir" && stagit "$repo_dir")
          done

          # 3. ルート目録（index.html）の生成
          (cd "${ROOT_DIR}/repositories" && stagit-index *.git > "${ROOT_DIR}/html/index.html")

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./html
